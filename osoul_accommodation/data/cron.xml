<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Scheduled action to update the difference in days -->
        <record id="ir_cron_update_difference_in_days" model="ir.cron">
            <field name="name">Update Difference in Days</field>
            <field name="model_id" ref="model_osoul_accommodation_building_dash"/>
            <field name="state">code</field>
            <field name="code">model._cron_update_difference_in_days()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active">True</field>
        </record>
    </data>
</odoo>
